# Agents

Use the freenet skill to validate and test contracts

Use `nix develop` to validate the nix shell works

Use `nix flake check` to validate if the flake is correct

If you are on nixos, which can be verified by checking for the existence of /etc/NIXOS, you can invoke `nix develop` before running any command. If that doesn't work, continue as normal.
